package Practice;

import java.util.*;
import java.util.LinkedList;
import java.util.Scanner;

/*Create a program where the user can enter values(Name, Creator, and Copies wanted) of a 
 * game and at the end ask the user whether or not they wish to add more values(games)
 * if no then return all the values the user inputed
 */
public class VideoGameList {

	public static void main(String[] args) {
		LinkedList<>  = new LinkedList<>();
		Scanner input = new Scanner(System.in);
		}
}
